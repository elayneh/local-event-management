query GetEvents {
  events(order_by: { created_at: asc }, where: { end_date: {} }) {
    address
    category_id
    created_at
    end_date
    description
    id
    is_free
    location
    price
    start_date
    title
    updated_at
    venue
  }
}
