mutation ChangePassword(
  $id: uuid!
  $currentPassword: String!
  $newPassword: String!
) {
  changePassword(
    changePasswordInput: {
      currentPassword: $currentPassword
      id: $id
      newPassword: $newPassword
    }
  ) {
    message
  }
}
