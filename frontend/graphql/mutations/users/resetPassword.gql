mutation PasswordReset($reset_token: String!, $new_password: String!) {
  updatePassword(
    updatePasswordInput: {
      new_password: $new_password
      reset_token: $reset_token
    }
  ) {
    message
  }
}
