mutation Register(
  $username: String!
  $email: String!
  $password: String!
  $role: String!
) {
  register(
    userInput: {
      username: $username
      email: $email
      password: $password
      role: $role
    }
  ) {
    token
    id
    role
  }
}
